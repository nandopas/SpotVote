<div class="page-title">
	<h1>All Users</h1>
</div>

<div class="container">
	<% @users.each do |user| %>
		<%= link_to user.username, user_path(user), class: "btn", style: "margin: 10px" %>
	<% end %>
</div>
